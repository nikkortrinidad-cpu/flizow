import { StubPage } from './StubPage';

export function OpsPage() {
  return (
    <StubPage
      viewClass="view-ops"
      title="Ops board"
      summary="Internal Kanban for hiring, finance, process, tooling — work the team is doing for the business itself."
    />
  );
}
