import { StubPage } from './StubPage';

export function WipPage() {
  return (
    <StubPage
      viewClass="view-wip"
      title="Weekly WIP"
      summary="Agenda builder with attendance, item queue, priority triage. Port lands after core directory work."
    />
  );
}
