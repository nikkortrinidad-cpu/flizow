import { StubPage } from './StubPage';

export function AnalyticsPage() {
  return (
    <StubPage
      viewClass="view-analytics"
      title="Delivery health"
      summary="Filters, KPI grid with drill-down, upcoming deliverables, team workload. Ported after the Clients directory."
    />
  );
}
