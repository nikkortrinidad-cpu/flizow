import { StubPage } from './StubPage';

export function ClientsPage() {
  return (
    <StubPage
      viewClass="view-clients"
      title="Clients directory"
      summary="Split-view list of clients with status chips, saved views, and detail pane. Port lands in the next pass."
    />
  );
}
